components:
  terraform:
    aws-kms/basic:
      metadata:
        component: target
      vars:
        enabled: true
        allowed_principal_arns:
          - "arn:aws:iam::000000000000:user/ForBar"
